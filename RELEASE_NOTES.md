<!--
本文件是「当前版本」的发布说明,由维护者在发版前手写,CI 直接取用它作为
GitHub Release 正文(body_path),因此:
  1. 只放本版内容,历史版本写进 CHANGELOG.md;
  2. 每段必须写在一行内,不要按列折行 —— GitHub 会把单个换行渲染成 <br>,
     折行会让正文右侧出现大片空白。
在本版内容之前不要添加 H1 标题,Release 页面已由标题字段显示版本号。
-->

修复版本,不涉及配置格式变更,**可直接覆盖升级**。

本版集中解决「选曲识别错误导致打错谱面」与「火罐不足退出游戏后又被自动拉起」两类问题。

## 修复内容

### 1. 同名曲目串谱面(`閃光` 等 8 组)

抽到 Roselia 版 `閃光`(曲目 id 410)时,脚本按另一版(id 467,Afterglow×レイヤ)的谱面打歌,整首完全对不上;两版 EXPERT 等级分别是 26 与 27。曲库中同名的 8 组曲目都有这个问题。

**原因**:曲库索引以曲名为键,同名时后写入的条目覆盖先前的。全库 818 首里有 8 组同名不同 id,每组只剩最后一个可达,另一首永远不可能被选中。

**修复**:索引改为保留每个曲名的全部同名 id(原默认项排最前,单条目曲目的行为保持不变)。当选曲命中同名条目且两条谱面不同时,读选歌界面上的「乐曲等级」数字来区分 —— 该数字等于高亮曲目在当前难度下的等级,而实测「等级可区分」与「谱面不同」完全等价;读不到或对不上任何一条时,放弃本次选曲并重新抽,不做猜测。

### 2. OCR 截断导致匹配到别的曲子(`ぎゅっDAYS♪` → `DAYS`)

`ぎゅっDAYS♪`(曲目 id 169)被识别成 `DAYS`(曲目 id 120),谱面打错。

**原因**:两个 OCR 模型的读数同时参与匹配,默认模型把标题截断成 `DAYS` —— 它恰好是另一首曲子的完整标题,相似度满分 100,反而压过正确条目的 93 分。

**修复**:标题先做 NFKC 归一(全角转半角),再按读数长度加权 —— 读数越短说明丢字越多、证据越弱,得分按比例下调。权重必须加在读数上而不是候选上,否则会改坏 `季節は次々死んでいく` 这类「正确条目比读数长」的正常截断。

### 3. 火罐不足退出游戏后,游戏在一分多钟后被自动重启

配置为「火罐为 0 就退出游戏」时,游戏确实被关闭,但约 1 分 45 秒后又自行启动,bot 继续选歌并再次检测到火罐为 0,如此循环。

**原因**:打歌环节的报错会归到父节点上。`ensure_liveboost` 是 `comfirm_song` 的 `next` 节点,它失败时框架归因到 `comfirm_song`,执行的是 `comfirm_song` 的错误分支而不是该节点自己写的「终止任务」分支,任务根本没有停;游戏被关掉后所有选歌节点都识别不到,流程落到主流程的中断列表里的「启动游戏」(该节点没有识别条件,必定命中),于是游戏被重新拉起。

**修复**:改为在自定义动作内显式执行「关闭游戏 + 终止任务」,「火罐不足」「生命值耗尽后等待手动操作」「结算失败次数超限」三处统一使用。

## 验证说明

- 离线回归测试 4 组,共 61 项断言全部通过(歌名匹配由 44 条扩充到 61 条,新增双模型读数、同名曲目消歧、索引完整性三部分)。
- 以选歌界面「乐曲等级」数字为真值,对真实运行日志中的 48 轮选曲 OCR 做回放评测:修复前 46/48,修复后 48/48。
- 任务终止语义单测:用无模拟器控制器跑完整 pipeline,修复后任务在 1.6 秒内正常结束;真机端到端复测,退出后再未出现游戏被重新拉起。
- 流水线资源校验通过(`Resource.loaded = True`),节点列表无重复元素。

## 已知问题

**极少数情况下可能出现脚本无响应**:当前奏静默时长不足,冻结判定迟迟无法确认时,等待首音的过程没有超时退出;此时若游戏弹出「演出失败」弹窗盖住识别区域,脚本会停在该弹窗上不再动作。遇到该情况请手动停止并重新启动脚本。表现特征为运行日志长时间重复输出 `wait_first` 的帧变化行且数值恒为 0.00。

## 下载

| 文件 | 说明 |
| --- | --- |
| `autodori_v1.2.4_Windows_X64.zip` | Windows 64 位完整包,解压即用 |

压缩包内含:

- `autodori_gui.exe` — 图形界面启动器(双击运行)
- `autodori.exe` — 打歌核心程序(GUI 自动拉起,无需手动运行)
- `assets/` — 识别资源、谱面模型、minitouch 触控二进制
- `README.md` / `CHANGELOG.md` / `RELEASE_NOTES.md` — 说明文档与更新日志
- `screenshots/` — 界面截图

## 升级方式

下载 zip 解压覆盖旧版本即可。配置与存档位于 `data/`、`cache/` 目录,不会被覆盖,升级无需重新设置。

## 使用前请注意

请务必按 `README.md` 的「环境要求」配置模拟器与游戏,其中 **MuMu 模拟器「禁用安卓系统声音」必须取消勾选** —— 这是打歌时基偏早(狂爆 FAST)最常见的原因。
