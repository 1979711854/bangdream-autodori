<!--
本文件是「当前版本」的发布说明,由维护者在发版前手写,CI 直接取用它作为
GitHub Release 正文(body_path),因此:
  1. 只放本版内容,历史版本写进 CHANGELOG.md;
  2. 每段必须写在一行内,不要按列折行 —— GitHub 会把单个换行渲染成 <br>,
     折行会让正文右侧出现大片空白。
在本版内容之前不要添加 H1 标题,Release 页面已由标题字段显示版本号。
-->

修复版本,不涉及配置格式变更,**可直接覆盖升级**。

本版集中解决「部分歌曲无法正常打歌」以及打歌中途被误判失败的问题。

## 修复内容

### 1. 部分歌曲在首音触发后约 30 毫秒被整首判失败

生命值充足的情况下,打歌刚开始就被判失败,日志尾部出现 `Failed when play song: 'precheck_rejected'`;实跑一轮 9 首里有 6 首如此。

**原因**:生命耗尽检测函数改为返回多状态字符串后,统计代码仍拿返回值直接做字典下标,而返回状态名 `precheck_rejected` 与统计键名 `precheck` 不同名,触发 `KeyError` 并被兜底异常捕获,整首歌被判失败。

**修复**:累计逻辑改为显式分支;并新增离线回归测试,断言检测函数返回的状态集合与调用方的分支集合始终一致。

### 2. 变体谱面被识别成基础曲(FULL / 3D / 平行曲)

`[FULL] キズナミュージック♪`(EXPERT 1485 音符)被识别为基础版 `キズナミュージック♪`(436 音符),长版只打完前 101 秒的短版谱面便停手,后半首全部 MISS。

**原因**:两个因素叠加。曲库索引只收录日文标题,而部分变体条目只有简中标题带标注,仅凭日文名无法与基础曲区分;且字符串相似度算法对「短候选被长查询包含」的情形封顶,OCR 读错的标题反而压过正确条目。

**修复**:索引补一层简中标题(不覆盖已有键);匹配时先按「标题是否带前缀标记」分池,再在同池内比相似度,池内最高分过低时回退全库重比。

### 3. 生命值耗尽后选择「等待手动操作」不生效

GUI 将「生命值耗尽后」设为等待,脚本处理完弹窗后仍继续打歌。

**原因**:与 v1.2.2 的火罐退出问题同因 —— 自定义动作内停止任务不可靠,动作返回后节点仍会沿后续节点继续。

**修复**:改为返回失败并由节点的错误分支真正终止任务。

### 4. GUI 运行途中修改的设置需重启才生效

「火罐为 0 时」「生命值耗尽后」等开关在运行中改动无效,只有选曲策略与 PHOTOGATE 校准能实时生效。

**修复**:统一改为实时重读 `data/config.yml`。

### 5. 结算阶段落在「演出失败」界面时未被识别

给打歌与结算相关节点补充失败弹窗的中断识别与错误分支,避免异常退出后任务悬停。

## 诊断能力增强

以下改动只增加日志、不改变打歌判定逻辑,便于反馈问题时定位。

- **生命检测汇总**:打歌结束时输出一行统计(执行次数 / 命中 / 亮度预检跳过 / OCR 异常 / 因切片过密跳过),可直接区分「检测没机会跑」「白底弹窗被预检漏掉」「OCR 没读出来」三类成因。
- **冻结与首音触发日志**:`冻结完成` 行补充总耗时、静默判定重置次数、检测带范围;新增 `首音触发` 行,记录带平均色判据与检测带顶部行(更灵敏判据)各自的触发时刻,两者之差即现有判据的滞后量。
- **手指占用告警**:5 指全被占用会导致部分音符根本不发出点击指令(表现为零星 MISS 且 FAST/SLOW 为 0),现会输出告警并列出受影响音符。

## 已知问题

**极少数情况下可能出现脚本无响应**:当前奏静默时长不足,冻结判定迟迟无法确认时,等待首音的过程没有超时退出;此时若游戏弹出「演出失败」弹窗盖住识别区域,脚本会停在该弹窗上不再动作。遇到该情况请手动停止并重新启动脚本。表现特征为运行日志长时间重复输出 `wait_first` 的帧变化行且数值恒为 0.00。

## 下载

| 文件 | 说明 |
| --- | --- |
| `autodori_v1.2.3_Windows_X64.zip` | Windows 64 位完整包,解压即用 |

压缩包内含:

- `autodori_gui.exe` — 图形界面启动器(双击运行)
- `autodori.exe` — 打歌核心程序(GUI 自动拉起,无需手动运行)
- `assets/` — 识别资源、谱面模型、minitouch 触控二进制
- `README.md` / `CHANGELOG.md` / `RELEASE_NOTES.md` — 说明文档与更新日志
- `screenshots/` — 界面截图

## 升级方式

下载 zip 解压覆盖旧版本即可。配置与存档位于 `data/`、`cache/` 目录,不会被覆盖,升级无需重新设置。

## 验证说明

- 离线回归测试 4 组(生命检测状态映射、歌名匹配 44 条真实 OCR 样本、挖矿档位判定、GUI 日志处理),共 44 项断言,全部通过。
- 流水线资源校验通过(`Resource.loaded = True`),节点列表无重复元素。
- 反编译核对发布包:打歌核心程序含本次全部修复与诊断标记,`finger_missed` 位于 `chart` 模块;GUI 启动器读出版本号 `1.2.3`;包内 `assets/` 与源码仓库逐文件比对一致。

## 使用前请注意

请务必按 `README.md` 的「环境要求」配置模拟器与游戏,其中 **MuMu 模拟器「禁用安卓系统声音」必须取消勾选** —— 这是打歌时基偏早(狂爆 FAST)最常见的原因。
