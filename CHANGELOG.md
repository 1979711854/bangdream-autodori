# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 与
[Conventional Commits](https://www.conventionalcommits.org/zh-hans/) 提交规范。

---

## [v1.2.4] - 2026-09-17

本次为 **缺陷修复版本**,不涉及配置格式变更,**可直接覆盖升级**。

### 修复

- **修复:同名曲目串谱面(`閃光` 等 8 组)**
  - 现象:抽到 Roselia 版 `閃光`(曲目 id 410)时,脚本按另一版(id 467,
    Afterglow×レイヤ)的谱面打歌,整首完全对不上;两版 EXPERT 等级分别是 26 / 27。
  - 原因:曲库索引以曲名为键,同名时后写入的条目覆盖先前的 —— 全库 818 首里有
    8 组同名不同 id,每组只剩最后一个可达,另一首永远不可能被选中。
  - 修复:新增 `ambiguous_titles` 记录每个曲名的全部同名 id(原默认项排最前,
    单条目曲目的行为保持不变);当选曲命中同名条目且两条谱面不同时,读选歌界面上
    的「乐曲等级」数字来区分 —— 该数字等于高亮曲目在当前难度下的等级,而实测
    「等级可区分」与「谱面不同」完全等价。读不到或对不上任何一条时,**放弃本次
    选曲并重新抽**,不做猜测。
  - 涉及文件:`src/autodori.py`

- **修复:OCR 截断导致匹配到别的曲子(`ぎゅっDAYS♪` → `DAYS`)**
  - 现象:`ぎゅっDAYS♪`(曲目 id 169)被识别成 `DAYS`(曲目 id 120),谱面打错。
  - 原因:两个 OCR 模型的读数同时参与匹配,默认模型把标题截断成 `DAYS` —— 它恰好
    是另一首曲子的**完整标题**,相似度满分 100,反而压过正确条目的 93 分。
  - 修复:标题先做 NFKC 归一(全角转半角),再按**读数长度**加权 —— 读数越短说明
    丢字越多、证据越弱,得分按比例下调。权重必须加在读数上而不是候选上,否则会
    改坏 `季節は次々死んでいく` 这类「正确条目比读数长」的正常截断。
  - 涉及文件:`src/autodori.py`

- **修复:火罐不足退出游戏后,游戏在一分多钟后被自动重启**
  - 现象:配置为「火罐为 0 就退出游戏」时,游戏确实被关闭,但约 1 分 45 秒后又
    自行启动,bot 继续选歌并再次检测到火罐为 0,如此循环。
  - 原因:环节报错会归到**父节点**上。`ensure_liveboost` 是 `comfirm_song` 的
    `next` 节点,它失败时框架归因到 `comfirm_song`,执行的是
    `comfirm_song.on_error`(`random_choice_song`)而不是该节点自己写的
    `on_error: "stop"`,任务根本没有终止;游戏被关掉后所有选歌节点都识别不到,
    流程落到 `main.interrupt` 里的 `start_app`(无识别条件 = 必定命中),
    于是游戏被重新拉起。
  - 修复:改为在自定义动作内显式执行「关闭游戏 + 终止任务」(新增
    `_exit_game_and_stop_task`),`HandleLiveBoost`、「生命值耗尽 / 等待手动操作」、
    结算失败次数超限三处统一使用。
  - 涉及文件:`src/autodori.py`

### 验证说明

- 离线回归测试 4 组,共 **61 项断言**全部通过(歌名匹配由 44 条扩充到 61 条,
  新增双模型读数、同名曲目消歧、索引完整性三部分)。
- 以选歌界面「乐曲等级」数字为真值,对真实运行日志中的 48 轮选曲 OCR 做回放评测:
  修复前 46/48,修复后 **48/48**。
- 任务终止语义单测:用无模拟器控制器跑完整 pipeline,修复后任务在 1.6 秒内正常
  结束;真机端到端复测,退出后再未出现游戏被重新拉起。
- 流水线资源校验通过(`Resource.loaded = True`),节点列表无重复元素。

### 发布物

- `autodori_v1.2.4_Windows_X64.zip`(解压即用)
  - `autodori_gui.exe` — 图形界面启动器
  - `autodori.exe` — 打歌核心程序(由 GUI 自动拉起)
  - `assets/` — 识别资源、谱面模型、minitouch 触控二进制
  - `README.md` / `CHANGELOG.md` / `RELEASE_NOTES.md` — 说明文档与更新日志
  - `screenshots/` — 界面截图

### 升级方式

直接下载 zip 解压覆盖旧版本即可(配置与存档保留在 `data/`、`cache/`
目录,不会被覆盖)。

### 已知问题

- **极少数情况下可能出现脚本无响应**:当前奏静默时长不足,冻结判定迟迟无法
  确认时,等待首音的过程没有超时退出;此时若游戏弹出「演出失败」弹窗盖住识别
  区域,脚本会停在该弹窗上不再动作。遇到该情况请手动停止并重新启动脚本。
  表现特征为运行日志长时间重复输出 `wait_first` 的帧变化行且数值恒为 0.00。

## [v1.2.3] - 2026-09-16

本次为 **缺陷修复版本**,不涉及配置格式变更,**可直接覆盖升级**。

### 修复

- **修复:部分歌曲在首音触发后约 30 毫秒被整首判失败**
  - 现象:生命值充足的情况下,打歌刚开始就被判失败,日志尾部出现
    `Failed when play song: 'precheck_rejected'`,当日一轮 9 首里 6 首如此。
  - 原因:生命耗尽检测函数改为返回多状态字符串后,统计代码仍拿返回值直接
    做字典下标(`life_stats[life_result]`),而返回状态名 `precheck_rejected`
    与统计键名 `precheck` 不同名 → `KeyError` 被 `Play.run` 的兜底异常捕获,
    整首歌被判失败。触发时机为每个存活检查点,首音触发后第一次检查即可能命中。
  - 修复:累计逻辑改为显式分支,新增状态不会再引发崩溃。同时新增离线回归
    测试,断言「检测函数返回的状态集合」与「调用方的分支集合」始终一致,
    且源码中不得再出现该形式的直接索引。
  - 涉及文件:`src/autodori.py`

- **修复:变体谱面被识别成基础曲(FULL / 3D / 平行曲)**
  - 现象:`[FULL] キズナミュージック♪`(EXPERT 1485 音符)被识别为基础版
    `キズナミュージック♪`(436 音符),长版只打完前 101 秒的短版谱面便停手,
    后半首全部 MISS。
  - 原因:两个因素叠加。其一,曲库索引只收录日文标题,而部分变体条目只有
    简中标题带标注(如 #484 的简中名为「(支持3D演出模式)」),仅凭日文名
    无法与基础曲区分;其二,`fuzzywuzzy` 的 `WRatio` 对「短候选被长查询包含」
    的情形封顶 90 分,OCR 读错的 `「FULLIキズナミュージックト` 反而以 90 分
    压过正确条目的 87 分。
  - 修复:索引在原有基础上补一层简中标题(`setdefault`,不覆盖已有键);
    匹配时先按「标题是否带前缀标记」分池,再在同池内比相似度,池内最高分低于
    60 分时回退全库重比。
  - 涉及文件:`src/autodori.py`

- **修复:生命值耗尽后选择「等待手动操作」不生效**
  - 现象:GUI 将「生命值耗尽后」设为等待,脚本处理完弹窗后仍继续打歌。
  - 原因:与 v1.2.2 的火罐退出问题同类,自定义动作内的 `run_action("stop")`
    不可靠 —— 动作返回 `True` 后节点仍会沿 `next` 继续。
  - 修复:改为 `return RunResult(False)`,并由 `handle_life_exhausted` 节点的
    `on_error: "stop"` 真正终止任务。
  - 涉及文件:`src/autodori.py`、`assets/resource/pipeline/live.json`

- **修复:GUI 运行途中修改的设置需重启才生效**
  - 现象:「火罐为 0 时」「生命值耗尽后」等开关在运行中改动无效,只有选曲策略
    与 PHOTOGATE 校准能实时生效。
  - 原因:这两处读的是模块级配置对象,仅在 import 时加载一次。
  - 修复:统一改为实时重读 `data/config.yml`(`_runtime_config()`),与选曲策略
    口径一致。
  - 涉及文件:`src/autodori.py`

- **修复:结算阶段落在「演出失败」界面时未被识别**
  - `playsong` / `wait_playresult` / `wait_playresult1` 节点补充 `live_failed`
    中断识别,并给 `playsong` 节点补充 `on_error`,避免异常退出后任务悬停。
  - 涉及文件:`assets/resource/pipeline/live.json`

### 诊断能力增强

以下改动只增加日志,不改变打歌判定逻辑,便于反馈问题时定位。

- **生命检测汇总**:打歌结束时输出一行统计(执行次数 / 命中 / 亮度预检跳过 /
  OCR 异常 / 因切片过密跳过),可直接区分「检测没机会跑」「白底弹窗被亮度预检
  漏掉」「OCR 没读出来」三类成因。
- **冻结与首音触发日志**:`冻结完成` 行补充总耗时、静默判定重置次数、检测带
  范围;新增 `首音触发` 行,记录带平均色判据与检测带顶部若干行(更灵敏判据)
  各自的触发时刻,两者之差即现有判据的滞后量。
- **手指占用告警**:谱面解析时 5 指全被占用会导致部分音符根本不发出点击指令
  (表现为零星 MISS 且 FAST/SLOW 为 0),现会输出告警并列出受影响音符。
- 涉及文件:`src/autodori.py`、`src/chart.py`、`src/util.py`

### 发布物

- `autodori_v1.2.3_Windows_X64.zip`(解压即用)
  - `autodori_gui.exe` — 图形界面启动器
  - `autodori.exe` — 打歌核心程序(由 GUI 自动拉起)
  - `assets/` — 识别资源、谱面模型、minitouch 触控二进制
  - `README.md` / `CHANGELOG.md` / `RELEASE_NOTES.md` — 说明文档与更新日志
  - `screenshots/` — 界面截图

### 升级方式

直接下载 zip 解压覆盖旧版本即可(配置与存档保留在 `data/`、`cache/`
目录,不会被覆盖)。

### 已知问题

- **极少数情况下可能出现脚本无响应**:当前奏静默时长不足,冻结判定迟迟无法
  确认时,等待首音的过程没有超时退出;此时若游戏弹出「演出失败」弹窗盖住识别
  区域,脚本会停在该弹窗上不再动作。遇到该情况请手动停止并重新启动脚本。
  表现特征为运行日志长时间重复输出 `wait_first` 的帧变化行且数值恒为 0.00。

### 验证说明

- 新增 / 保留离线回归测试 4 组(生命检测状态映射、歌名匹配 44 条真实 OCR
  样本、挖矿档位判定、GUI 日志处理),共 44 项断言,全部通过。
- 流水线资源校验通过(`Resource.loaded = True`),节点列表无重复元素。
- 发布包已逐项核对:
  - 反编译 `autodori.exe`,主脚本含本次全部修复与诊断标记
    (`_runtime_config`、`fuzzy_match_song`、`_has_title_prefix`、
    `_POOL_SCORE_FLOOR`、`wait_first_note`、生命检测汇总 / 冻结完成 /
    首音触发),`finger_missed` 位于 `chart` 模块;
  - 反编译 `autodori_gui.exe`,读出版本号 `1.2.3`;
  - 包内 `assets/` 与源码仓库逐文件比对一致,`assets/resource/pipeline/
    live.json` 与仓库版本 md5 相同。

---

## [v1.2.2] - 2026-09-13

本次为 **缺陷修复版本**,不涉及配置格式变更,**可直接覆盖升级**。

### 修复

- **修复:火罐为 0 且选择「退出游戏」时仍继续打歌**
  - 现象:火罐(Boost)耗尽、GUI 火罐选项设为「退出游戏」时,脚本不会退出,
    而是无视设置继续选曲打歌。
  - 原因:`HandleLiveBoost` 自定义动作在退出分支调用 `run_action("stop")`,
    但 MaaFramework 在动作返回 `True` 后仍会沿节点 `next` 继续,`stop`
    不会被可靠触发。
  - 修复:退出分支改为 `return RunResult(False)`,由节点的 `on_error: "stop"`
    真正终止任务(该机制在同类节点已验证可靠)。
  - 涉及文件:`src/autodori.py`、`assets/resource/pipeline/live.json`

- **修复:超过最大失败次数时未正确停止**
  - 现象:失败后重试超过上限,脚本仍不停止。
  - 原因:与上述问题同类 —— `SavePlayResult` 在超限分支同样依赖
    `run_action("stop")` 无效。
  - 修复:改为 `return RunResult(False)` 并给 `save_failed_playresult`
    节点加 `on_error: "stop"`。
  - 涉及文件:`src/autodori.py`、`assets/resource/pipeline/live.json`

### 发布物

- `autodori_v1.2.2_Windows_X64.zip`(解压即用)
  - `autodori_gui.exe` — 图形界面启动器
  - `autodori.exe` — 打歌核心程序(由 GUI 自动拉起)
  - `assets/` — 识别资源、谱面模型、minitouch 触控二进制
  - `README.md` / `screenshots/` — 说明文档与界面截图

### 升级方式

直接下载 zip 解压覆盖旧版本即可(配置与存档保留在 `data/`、`cache/`
目录,不会被覆盖)。

### 验证说明

- 已反编译核对发布包内二进制,确认版本号 `v1.2.2` 与火罐退出修复均正确包含。
- GUI / 打歌核心程序启动均通过冒烟测试。

---

## [v1.2.1] - 2026-09-10

本次为 **缺陷修复版本**,不涉及新功能与配置格式变更,**可直接覆盖升级**。

### 修复

- **修复:打歌结算信息缺少时间戳**
  - 现象:运行日志中「OK 结算 · 分数 2541856 · COMBO 603 · PERFECT 667 ·
    GREAT 1(FAST 1/SLOW 0) · GOOD」等结算行,时间列显示为空白。
  - 原因:GUI 在输出结算信息时未传入时间戳,导致该行时间字段为空。
  - 修复:在日志输出入口统一兜底 —— 未显式提供时间戳时自动补当前时刻。
    由于是入口级修复,**启动命令、photogate 校准、手动停止、错误提示**
    等其他日志行的时间戳一并恢复正常;显式传入的时间戳仍原样保留。
  - 涉及文件:`gui.py`

- **修复:选曲后歌名未实时显示(直到打歌结束才出现)**
  - 现象:选好歌曲后,运行日志与「当前曲目」要等到打歌结束后才显示歌名,
    期间无任何反馈,难以确认脚本是否选歌成功。
  - 原因:歌名日志的输出位置在**谱面解析之后**。脚本需先拉取谱面并把上万个
    音符解算为触控指令(实测耗时 5~15 秒),日志写在这批重活之后,导致歌名
    被推迟到「打歌即将开始」才产出。
    实测样本:`Save song` 于 19:14:03 记录,`Start play` 于 19:14:18 ——
    中间 15 秒空窗。
  - 修复:
    1. 脚本侧将歌名日志提前到歌名确定之后、谱面解析之前,并提升为 INFO 级别
       确保不会被日志过滤;
    2. GUI 侧在收到歌名后,立即更新「当前曲目」并追加一条 `选曲 · 歌名`
       日志,且对同一首歌去重,避免重复刷屏。
  - 涉及文件:`src/autodori.py`、`gui.py`

### 发布物

- `autodori_v1.2.1_Windows_X64.zip`(约 146 MB,解压即用)
  - `autodori_gui.exe` — 图形界面启动器
  - `autodori.exe` — 打歌核心程序(由 GUI 自动拉起)
  - `assets/` — 识别资源、谱面模型、minitouch 触控二进制
  - `README.md` / `screenshots/` — 说明文档与界面截图

### 升级方式

直接下载 zip 解压覆盖旧版本即可(配置与存档保留在 `data/`、`cache/`
目录,不会被覆盖)。

### 验证说明

- 新增离线回归测试,覆盖选曲实时性、去重、换歌、结算时间戳、异常行兜底
  等 15 项断言,全部通过。
- 已反编译核对发布包内二进制,确认上述修复与版本号均正确包含。
- GUI 启动与打歌核心程序启动均通过冒烟测试。

---

## [v1.2.0] - 2026-09-07

### 新增

- **环境自检**:启动时检查音频设置 / 电源计划 / 帧率 / 内存,提前发现会
  导致打歌漂移的环境问题。
- 打歌时序鲁棒性改进。

### 修复

- 帧率读取改用 `fps_limit_real`(高帧率模式实际渲染值);
  电源计划按 GUID 判定档位并显示真实名称。
- 运行日志可复制;电源计划检测行精简。

---

## [v1.1.x] - 历史版本

详见 GitHub Releases 历史记录。
